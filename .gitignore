__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# experiment output and datasets (only at the repository root:
# docs/results/ holds the committed summaries)
/results/
/runs/

# Datasets are fetched per machine, never committed. The download helper and
# this file are the only things under data/ that belong in the repository.
/data/*
!/data/download_bossbase.py
!/data/README.md
*.ckpt
*.pt

# images produced by examples/demo.py
examples/*.png

# The original script this project grew out of, archived in legacy/.
/decoder.py

# Reference implementations from the Binghamton DDE Lab. They are downloaded
# per machine (see matlab/README.md) and are not ours to redistribute.
matlab/WOW.m
matlab/S_UNIWARD.m
matlab/*.mexw64
matlab/*.mexa64
matlab/*.mexmaci64
